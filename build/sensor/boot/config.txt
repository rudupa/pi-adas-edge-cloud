# Raspberry Pi Zero W boot configuration — Sensor node

[pi0w]
kernel=kernel.img
dtoverlay=disable-bt
dtparam=i2c_arm=on
dtparam=spi=on
dtparam=audio=on
# Load compiled voice-bonnet overlay (voice-bonnet.dtbo must be in /boot/overlays/)
dtoverlay=voice-bonnet
enable_uart=1
gpu_mem=16
# Overclock settings (opt-in only; may reduce stability):
# arm_freq=1000
# over_voltage=4
