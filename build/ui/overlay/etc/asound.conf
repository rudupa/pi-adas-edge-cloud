# ALSA configuration for Pimoroni Pirate Audio (MAX98357A I2S amplifier)

pcm.!default {
    type plug
    slave {
        pcm "hw:0,0"
        rate 48000
        channels 2
        format S16_LE
    }
}

ctl.!default {
    type hw
    card 0
}
